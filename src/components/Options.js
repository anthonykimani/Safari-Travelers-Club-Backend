const Options = ({name}) => {
  return (
    <div>
      <button className="text-gray-500 border-solid border rounded-3xl px-4 py-1 border-gray-500 mx-2">
        {name}
      </button>
    </div>
  );
};

export default Options;
